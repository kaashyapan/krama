{
  "name": "fable",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "fable": "fable . --noCache",
    "benchmarks": "pnpm run fable && pnpm run build && node test/performance.js"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^4.3.8",
    "vitest": "^0.31.1"
  },
  "dependencies": {
    "benchtable": "^0.1.0"
  }
}
